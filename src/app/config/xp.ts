export interface xp {
    brokeStreak: number;
    streak: number;
    correctAnswer: number;
    incorrectAnswer: number;
}

export const XP : xp = {
    brokeStreak: -2,
    streak: 3,
    correctAnswer: 1,
    incorrectAnswer: -1,
};